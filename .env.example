# Storage backend: "gcs" or "local"
STORAGE_BACKEND=local

# Google Cloud Storage settings (required when STORAGE_BACKEND=gcs)
GCS_BUCKET_NAME=your-bucket-name

# For GCS, also set GOOGLE_APPLICATION_CREDENTIALS environment variable
# pointing to your service account JSON key file
